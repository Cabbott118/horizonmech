// Components
import Logout from '../features/authentication/components/Logout';

const Profile = () => {
  return (
    <div>
      <p>Profile</p>
      <Logout />
    </div>
  );
};

export default Profile;
